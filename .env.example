###########################################
# Authentication settings
###########################################

# Secret used for session encryption / JWT signing
AUTH_SECRET=

# GitHub OAuth app credentials (for social login)
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Google OAuth app credentials
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=


###########################################
# Database connection settings
###########################################

# Connection string for app to connect via pooling (e.g., with Supabase)
DATABASE_URL=

# Direct database connection string (used only for migrations, not for app runtime)
DIRECT_URL=

# Note: If you’re running a local Postgres instance in Docker, you will need to provide:
# DATABASE_URL=  <-- uncomment and set when using Docker Postgres
